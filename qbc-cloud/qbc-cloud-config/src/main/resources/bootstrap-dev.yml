server:
  port: 8888
spring:
  application:
    name: qbc-cloud-config
  cloud:
    config:
      server:
        git:
          searchPaths: qbc-cloud/config
          uri: https://github.com/bashuyi/qbc.git
