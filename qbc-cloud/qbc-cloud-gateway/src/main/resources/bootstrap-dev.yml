eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/
spring:
  application:
    name: qbc-cloud-gateway
  cloud:
    config:
      discovery:
        enabled: true
        serviceId: qbc-cloud-config
